name: test
on: push

jobs:
  generate_tag:
    runs-on: ubuntu-latest
    steps:
      - name: Generate build number
        uses: build-number/build-number 
        with:
            token: ${{secrets.github_token}}        
      - name: Print new build number
        run: echo "Build number is $BUILD_NUMBER"